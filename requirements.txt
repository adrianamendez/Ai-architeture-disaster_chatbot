# Slim dependencies for the Streamlit Cloud deployment.
# The Streamlit dashboard renders pre-computed results from streamlit_data.py
# and does NOT call Ollama, ChromaDB, sentence-transformers, etc.
# Keeping this file minimal makes Streamlit Cloud installs fast and avoids
# heavy ML libraries that often fail to compile on the deploy runner.
#
# To run the notebook (disaster_chatbot.ipynb) locally with the full stack
# (Ollama, ChromaDB, embeddings, agent, evaluator) install requirements-notebook.txt
# instead.

streamlit>=1.40.0,<2
plotly>=5.18.0,<7
pandas>=2.0.0,<3
numpy>=1.24.0,<3
